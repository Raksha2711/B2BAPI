using Data.Entity;
using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;
//using Microsoft.AspNetCore.Authentication;

namespace Admin.BusinessService.Contract
{
    //public class ILoginService
    //{
    //    Task<LoginViewModel> Login(string username, string otp);
    //    LoginViewModel Login(AuthenticateResult authenticateResult);
    //    Task<LoginViewModel> Login(Customer customer);
    //    LoginViewModel SignUp(Customer entity);
    //    Task<bool> GenerateOTP(string username);
    //    Task<bool> ResetPassword(string username, string password, string otp);
    //    bool UserExist(string username);
    //}
}
